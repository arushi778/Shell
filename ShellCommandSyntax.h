#ifndef __SHELL_COMMAND_SYNTAX_H__
#define __SHELL_COMMAND_SYNTAX_H__

// Analyze the command for syntax error's
// Not complicated compared to a compiler syntax analysis
// But you still need to create a rule tree to analyze the command for syntax


#include "CommonHeaders.h"
#include "ShellErrors.h"

namespace MyShell
{
	class CShellCommandSyntax
	{

	};
}

#endif		/* __SHELL_COMMAND_SYNTAX_H__ */
